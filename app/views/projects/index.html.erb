<div>
  <h1>All Projects</h1>
  <% @projects.each do |project| %>
    <div class="project">
      <h1 class="title"><%= link_to project.title, project_path(project) %></h1>
      <hr>
      <%= image_tag(project.image) if project.image.attached? %>
    </div>
  <% end %>
</div>
